version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - mvn install
      - mvn clean  
      - mvn package
artifacts:
  discard-paths: yes
  files:
    - target/*
    - scripts/*
    - appspec.yml
